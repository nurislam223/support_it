version: '3.8'

services:
  web:
    build: .
    ports:
      - "8082:8082"
    environment:
      - DATABASE_URL=postgresql://admin:admin@db:5432/users
      - DEBUG=false
    restart: unless-stopped

